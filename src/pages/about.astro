---
import Layout from "../layouts/Layout.astro";
import PagePlaceholder from "../components/PagePlaceholder.astro";
---

<Layout title="About — Lawrence Job" crumbs={[{ label: "About" }]}>
	<PagePlaceholder
		title="About"
		lede="Electronics, then embedded, then distributed systems, then leadership. The arc, not the CV."
	/>
</Layout>
