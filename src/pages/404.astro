---
import Layout from "../layouts/Layout.astro";
import PagePlaceholder from "../components/PagePlaceholder.astro";
---

<Layout title="Not found — Lawrence Job" crumbs={[{ label: "Not found" }]}>
	<PagePlaceholder
		eyebrow="404"
		title="Not found"
		lede="There’s nothing at this address — check the URL, or start again from the homepage."
		mediaLabel="not found"
	/>
</Layout>
